name: CI

# Run CI on pushes to main
trigger:
  - main

# Run CI on pull requests targeting main
pr:
  - main

variables:
  CI: 'true'

  # ---------------- PR RUN ----------------
  ${{ if eq(variables['Build.Reason'], 'PullRequest') }}:
    NX_BRANCH: $(System.PullRequest.PullRequestNumber)
    TARGET_BRANCH: $[replace(variables['System.PullRequest.TargetBranch'], 'refs/heads/', 'origin/')]

  # --------------- MAIN RUN ---------------
  ${{ if ne(variables['Build.Reason'], 'PullRequest') }}:
    NX_BRANCH: $(Build.SourceBranchName)

jobs:
  - job: main
    pool:
      name: 'selfhosted'

    steps:
      # Full checkout so BASE_SHA works for Nx
      - checkout: self
        fetchDepth: 0
        fetchFilter: tree:0

      # Configure Azure DevOps CLI defaults (org/project)
      - bash: az devops configure --defaults organization=$(System.TeamFoundationCollectionUri) project=$(System.TeamProject)
        displayName: 'Set AzDo defaults'

      # Calculate BASE_SHA for PR
      - bash: |
          TARGET_BRANCH=$(echo "$(System.PullRequest.TargetBranch)" | sed 's|refs/heads/||')
          BASE_SHA=$(git merge-base origin/$TARGET_BRANCH HEAD)
          echo "##vso[task.setvariable variable=BASE_SHA]$BASE_SHA"
          echo "Base SHA for PR: $BASE_SHA"
        displayName: 'Calculate BASE_SHA (PR)'
        condition: eq(variables['Build.Reason'], 'PullRequest')

      # Calculate BASE_SHA for main branch (try last successful, fallback to HEAD~1)
      - bash: |
          LAST_SHA=$(az pipelines build list \
            --branch $(Build.SourceBranchName) \
            --definition-ids $(System.DefinitionId) \
            --result succeeded \
            --top 1 \
            --query "[0].triggerInfo.\"ci.sourceSha\"" \
            --output tsv)

          if [ -z "$LAST_SHA" ] || [ "$LAST_SHA" == "null" ]
          then
            BASE_SHA=$(git rev-parse HEAD~1)
            echo "Last successful commit not found. Using fallback HEAD~1: $BASE_SHA"
          else
            BASE_SHA=$LAST_SHA
            echo "Last successful commit SHA: $BASE_SHA"
          fi
          
          echo "##vso[task.setvariable variable=BASE_SHA]$BASE_SHA"
        displayName: 'Calculate BASE_SHA (main)'
        condition: ne(variables['Build.Reason'], 'PullRequest')
        env:
          AZURE_DEVOPS_EXT_PAT: $(System.AccessToken)

      # Calculate HEAD_SHA
      - bash: |
          HEAD_SHA=$(git rev-parse HEAD)
          echo "##vso[task.setvariable variable=HEAD_SHA]$HEAD_SHA"
          echo "Head SHA: $HEAD_SHA"
        displayName: 'Calculate HEAD_SHA'

      # Debug: Print all variables
      - bash: |
          echo "Build Reason: $(Build.Reason)"
          echo "NX_BRANCH: $(NX_BRANCH)"
          echo "BASE_SHA: $(BASE_SHA)"
          echo "HEAD_SHA: $(HEAD_SHA)"
        displayName: 'Debug: Print variables'

      # ---------------------
      # Install dependencies
      # ---------------------
      - script: npm ci --legacy-peer-deps
        displayName: 'Install dependencies'

      # Needed only for PRs (allows merge-base to work cleanly)
      - script: git branch --track main origin/main
        condition: eq(variables['Build.Reason'], 'PullRequest')
        displayName: 'Track main (PR only)'

      # ---------------------
      # Nx affected (NO DTE)
      # ---------------------
      - script: npx nx affected --base=$(BASE_SHA) --head=$(HEAD_SHA) -t lint test build --no-dte
        displayName: 'Nx affected (lint, test, build)'
        env:
          NX_CLOUD_DISTRIBUTED_EXECUTION: 'false'
          NX_BRANCH: $(NX_BRANCH)

      # ---------------------
      # Nx Self-Healing CI
      # ---------------------
      - script: npx nx-cloud fix-ci
        displayName: 'Nx Cloud Self-Healing CI'
        condition: always()
        env:
          NX_BRANCH: $(NX_BRANCH)